let initialState = {
  friendsData: [
    {
      name: "Illya",
      id: 1,
    },
    {
      name: "Sonya",
      id: 2,
    },
    {
      name: "Tom",
      id: 3,
    },
  ],
};

function navbarReducer(state = initialState, action) {
  return state;
}

export default navbarReducer;
